<!--
  This assignment was used for the Fall 2022 term.
-->
<section xml:id="section-assignment-week3">
  <title>Week 3 Assignment</title>
  <p><ol>
    <li>
      <p>
        Write the first vector as a linear combination of the second
        and third vectors or show that it cannot be done.(8)
        <ol>
          <li>
            <me>
              \begin{pmatrix}
                9 \\ 0 
              \end{pmatrix},
              \begin{pmatrix}
                -2 \\ 0 
              \end{pmatrix},
              \begin{pmatrix}
                1  \\ 1 
              \end{pmatrix}
            </me>
          </li>
          <li>
            <me>
              \begin{pmatrix}
                6 \\ -8 \\ 2
              \end{pmatrix},
              \begin{pmatrix}
                1 \\ -4 \\ -3 
              \end{pmatrix},
              \begin{pmatrix}
                -1 \\ 0 \\ -2 
              \end{pmatrix}
            </me>
          </li>
        </ol>
      </p>
    </li>
    <li>
      <p>
        Write a third vector which is linearly independent to the
        first two. (Make sure you show me why it is linearly
        independent.) (6) 
        <ol>
          <li>
            <me>
              \begin{pmatrix}
                -7 \\ -1 \\ -3  
              \end{pmatrix},
              \begin{pmatrix}
                3 \\ 0 \\ -4 
              \end{pmatrix}
            </me>
          </li>
          <li>
            <me>
              \begin{pmatrix}
                5 \\ 5 \\ 5 
              \end{pmatrix},
              \begin{pmatrix}
                -3 \\ -2 \\ -7 
              \end{pmatrix}
            </me>
          </li>
        </ol>
      </p>
    </li>
    <li>
      <p>
        Write equations for these planes.(10) 
        <ol>
          <li>
            The plane with normal 
            <m>\begin{pmatrix}
              -4 \\ -6 \\ -7 
            \end{pmatrix}</m>
            containing the point
            <m>\begin{pmatrix}
              0 \\ 1 \\ -1 
            \end{pmatrix}</m>.
          </li>
          <li>
            The plane containing the points 
            <m>\begin{pmatrix}
              2 \\ 1 \\ -2 
            \end{pmatrix}, </m>
            <m>\begin{pmatrix}
              3 \\ -1 \\ -2 
            \end{pmatrix}</m> and 
            <m>\begin{pmatrix}
              3 \\ -5 \\ 3 
            \end{pmatrix}</m>.
          </li>
        </ol>
      </p>
    </li>
  </ol></p>
</section>
